package rgou.view;

/**
 * Enum representing various scenes in the game.
 */
public enum GameScenes {
	LOADING,
	SELECT_REMOTE,
	SELECT_REMOTE_HOST,
	SELECT_REMOTE_JOIN,
	MAIN_MENU,
	GAMEPLAY,
	GAME_END
}
